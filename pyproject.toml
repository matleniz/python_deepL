[project]
name = "python-deepl"
version = "0.1.0"
description = "Python / DeepL — agents ml-arena (PettingZoo)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ipykernel>=7.3.0",
    "jupyter>=1.1.1",
    "mlarena-sdk==0.3.0",
    "numpy>=2.5.3",
    "python-dotenv>=1.2.3",
]
